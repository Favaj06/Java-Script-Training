// In Node.js, JavaScript runs directly in the terminal without a browser.
// In a browser, JavaScript runs inside a web page and can work with HTML and CSS.

const message = "Hello from Node.js";
console.log(message);

const fruits = ["apple", "banana", "mango"];
const upper = fruits.map(f => f.toUpperCase());

console.log(upper);

// REPL stands for Read-Eval-Print Loop.
// It is useful for quickly testing JavaScript code or trying out ideas
// without creating and running a separate file.