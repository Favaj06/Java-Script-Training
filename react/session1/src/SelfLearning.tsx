function SelfLearning() {
  return (
    <div>
      <h2>Self Learning Tasks</h2>
      <p>Check the comments in this file.</p>
    </div>
  )
}

/*
1. React.StrictMode
React.StrictMode helps identify potential problems during development.
It only runs in development mode and does not affect production.
*/

/*
2. Controlled vs Uncontrolled Components
Controlled components are managed by React state.
Uncontrolled components manage their own state using the DOM.
*/

/*
3. key Prop
The key prop helps React identify list items efficiently.
Use a unique id instead of the array index whenever possible.
*/

/*
4. Fragments
Fragments group multiple elements without creating an extra HTML element.
Use <React.Fragment key={id}> when a Fragment needs a key.
*/

export default SelfLearning